name: ordna-frontend
root: ~/Sites/ordna-frontend/
pre_window:
  - yarn -v
  - export AWS_PROFILE=ordna

windows:
  - ordna-frontend:
      layout: even-horizontal
      panes:
        - c
        - yarn && bundle && yarn start
