name: ordna-frontend
root: ~/Projects/ordna-frontend/
pre_window:
  - node -v
  - yarn -v
  - export AWS_PROFILE=ordna

windows:
  - ordna-frontend:
      layout: even-horizontal
      panes:
        - c
        - yarn && bundle && yarn start
