name: ordna-planner
root: ~/Projects/ordna-planner/
pre_window:
  - node -v
  - yarn -v
  - export AWS_PROFILE=ordna

windows:
  - ordna-planner:
      layout: even-horizontal
      panes:
        - c
        - yarn && yarn start
