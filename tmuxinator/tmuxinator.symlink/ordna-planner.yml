name: ordna-planner
root: ~/Sites/ordna-planner/
pre_window:
  - node -v
  - yarn -v
  - export AWS_PROFILE=ordna

windows:
  - ordna-planner:
      layout: even-horizontal
      panes:
        - c
        - yarn && yarn start
